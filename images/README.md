Drop chart screenshots here (e.g. `cohort_heatmap.png`, `revenue_trend.png`,
`delivery_delay_by_state.png`) and reference them in the main README with:

```markdown
![Cohort Retention Heatmap](images/cohort_heatmap.png)
```
