# notebooks/

Optional: put a `visuals.ipynb` here that connects to your Postgres database
(e.g. via `sqlalchemy` + `pandas.read_sql`) and charts the output of each
query in `sql/03`–`06` using matplotlib/seaborn. Export the charts as PNGs
into `../images/` and embed them in the main README.
